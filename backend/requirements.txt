fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
networkx>=3.3
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
httpx>=0.27.0
pydantic>=2.7
pydantic-settings>=2.3
numpy>=2.0
scipy>=1.13
python-multipart>=0.0.9
python-dotenv>=1.0
# Layer 2 — Tier 2 Neo4j backend (optional; system falls back to Tier 1 if unavailable)
neo4j>=5.0

# Layer 1 — Real data collectors (all optional; system falls back to simulation)
netmiko>=4.3          # SSH/telnet to routers
pyshark>=0.6          # tshark packet capture (needs Wireshark installed)
scapy>=2.5            # ICMP traceroute (needs Npcap on Windows)
